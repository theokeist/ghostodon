# pnpm settings for monorepo dev ergonomics (especially on Windows)
node-linker=hoisted
# Avoid surprises from strict peer deps during early-stage prototyping
strict-peer-dependencies=false
store-dir=.pnpm-store
